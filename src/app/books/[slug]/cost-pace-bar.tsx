"use client";

// Cost pace bar — shows cumulative LLM spend vs. the book ceiling.
// Currently a stub until the LLMCallLog migration is applied.
export function CostPaceBar({ slug: _slug }: { slug: string }) {
  return null;
}
